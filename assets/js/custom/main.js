// Border Carousel

$('.border-carousel').owlCarousel({
    loop:true,
    // rewind: false,
    autoplay: true,
    autoplayTimeout: 0,
    smartSpeed: 3000,
    autoplaySpeed: 3000,
    slideTransition: 'linear',
    margin:10,
    nav:false,
    dots: false,
    items:30
})

// Border Carousel End Here